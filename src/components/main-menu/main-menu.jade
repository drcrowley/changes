mixin main-menu(menu, activeIndex)
	nav.main-menu&attributes(attributes)
		ul.main-menu__list
			each item, index in menu
				li.main-menu__item(class= (index == activeIndex || 0 ? 'main-menu__item_active' : ''))
					a(href="#").main-menu__link= item